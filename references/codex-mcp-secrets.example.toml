# Copy this file to .codex/mcp-secrets.toml in the source checkout.
# Replace every placeholder locally, keep the real file out of Git, and set
# mode 0600 on macOS. The sync script accepts only env and http_headers tables
# here, then writes the merged result to the user's ~/.codex/config.toml.

[mcp_servers.context7.env]
CONTEXT7_API_KEY = "***REDACTED***"
