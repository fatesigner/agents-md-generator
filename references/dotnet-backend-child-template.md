# [后端项目名] 子目录指南

本文件作用于 `[后端目录]` 及其子目录。未特别说明的事项，继承仓库根目录 `AGENTS.md`。

## 项目结构

__BACKEND_STRUCTURE_OVERVIEW__

## 后端改动边界

- `[MUST]` 优先沿用现有分层：[后端分层描述]。
- `[MUST]` `[入口项目]` 仅承载启动、配置、宿主和 HTTP 管道逻辑。
- `[MUST NOT]` 将核心业务规则堆到 `Program.cs`、`Startup.cs`、`ServiceEngine.cs` 或 HTTP 入口层。
- `[MUST]` 业务流程与模块编排放在 `[业务服务项目]` 对应模块目录（如 `[后端模块示例]`）。
- `[MUST]` 仓储、数据库访问、缓存细节放在 `[数据访问项目]`，不在业务层散写数据访问。
- `[MUST]` 涉及配置、模板、缓存、日志改动时，同步检查 `[关键配置目录与文件]` 与读取代码。

### 生成目录与高风险 touchpoint

__GENERATED_BOUNDARIES__

## 命名与模型约定

- `[MUST]` 合同、接口、共享模型优先放在 `[业务契约项目]` 或既有契约项目。
- `[MUST NOT]` 在入口层或仓储层重复声明已存在契约。
- `[MUST]` 后端函数/类保持单一职责，避免在同一实现混合多类业务意图。
- `[DEFAULT]` 同类逻辑重复达到 3 处及以上时，再评估通用化抽象。
- `[MUST]` 新增服务类、处理器、网关类、配置类时复用现有模块术语与目录命名。
- `[MUST]` Web/业务/仓储共享概念统一命名，避免同字段多命名。
- `[MUST]` [SQL命名规范]

## 配置与启动约定

- `[MUST]` 新增配置项时说明用途、默认行为、是否必填、受影响入口或模块。
- `[MUST]` 涉及启动流程、HTTP、中间件、服务注册、宿主行为改动时，优先检查 `[入口项目]` 启动文件与相关配置。
- `[MUST]` 涉及日志、缓存、模板导出、消息中间件、ID 生成改动时，明确受影响配置与运行时行为。

### 配置 touchpoint

__CONFIG_TOUCHPOINT_DETAILS__

## 依赖与数据边界

- `[MUST]` 涉及数据库结构、实体映射、仓储查询、事务改动时，同步检查 `[数据访问项目]`、迁移项目、相关数据契约。
- `[MUST]` [SQL工具链约束]
- `[MUST]` [SQL脚本目录约束]
- `[MUST]` 结构变更脚本默认放在 `db/migrations`；若项目已有等价迁移目录，则复用既有目录，不新建平行体系。
- `[MUST]` 初始化或种子数据脚本默认放在 `db/seeds` 或 `db/init`，不得混入 `db/migrations`。
- `[MUST]` 一次性修复、补数或运维脚本默认放在 `db/patches`，不得伪装成正式 migration。
- `[MUST]` 一个正式 migration 脚本只承载一次独立结构变更，不在同一文件混合 schema 变更、补数、排查查询或人工执行说明。
- `[MUST]` 涉及缓存、消息队列、第三方 SDK、文件存储、外部服务调用时，明确依赖入口、配置来源、失败处理路径。
- `[MUST NOT]` 在业务层、入口层、控制器绕过既有封装直接访问底层数据源或基础设施。
- `[MUST NOT]` 未确认固定归档位置前，不在功能目录、仓储目录、临时目录或项目根目录零散创建 `.sql` 文件。
- `[MUST]` 涉及 `DROP`、`ALTER COLUMN`、批量 `UPDATE/DELETE` 或其他高风险 SQL 时，在交付中说明影响范围、回滚思路与执行前置条件。
- `[MUST]` 新增外部依赖或调整接入方式时，在交付中说明受影响模块、配置项、回滚关注点。

## 验证要求

- `[MUST]` 至少执行受影响项目 `dotnet build`。
- `[MUST]` 沿用本子项目既有断言风格与测试夹具结构。
- `[MUST]` 不新增脱离现有构建链的自定义脚本（除非任务明确要求）。
- `[DEFAULT]` 涉及入口层改动时优先验证：
  - `[入口项目路径]`
- `[DEFAULT]` 涉及入口启动链路改动时，补充最小运行验证（如 `dotnet run` 或等效启动验证）。
- `[DEFAULT]` 涉及业务层或仓储层改动时优先构建对应 `.csproj`，必要时回到解决方案级构建。
- `[MUST]` 涉及配置改动时，在交付中说明受影响配置文件、默认值行为、部署注意事项。

### 验证矩阵

__VALIDATION_MATRIX__

## 常用命令

```powershell
[后端命令 1]
[后端命令 2]
[后端命令 3]
[后端命令 4]
[后端命令 5]
```
